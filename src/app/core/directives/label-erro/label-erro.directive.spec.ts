import { LabelErroDirective } from './label-erro.directive';

describe('LabelErroDirective', () => {
  it('should create an instance', () => {
    const directive = new LabelErroDirective();
    expect(directive).toBeTruthy();
  });
});
